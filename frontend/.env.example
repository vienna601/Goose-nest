# Where the dev server proxies /api/* to (the FastAPI backend).
VITE_API_TARGET=http://127.0.0.1:8000

# Force the simulated contact agent even if the backend has /inquiries.
# Useful for rehearsing the demo without spending Steel/Gemini quota.
VITE_MOCK_AGENT=false
