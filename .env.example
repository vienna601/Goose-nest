# Copy to .env and fill in. .env is gitignored; never commit it.

# Supabase -> Project Settings -> API Keys
SUPABASE_URL=https://<your-ref>.supabase.co
SUPABASE_SECRET_KEY=sb_secret_...          # server only. Bypasses RLS.
SUPABASE_PUBLISHABLE_KEY=sb_publishable_... # safe in B's frontend bundle

# Goes in the scraper's User-Agent so a site owner can reach us.
SCRAPER_CONTACT_EMAIL=you@example.com

# Agent (A11+)
STEEL_API_KEY=                 # app.steel.dev -> API keys
GEMINI_API_KEY=                # aistudio.google.com -> Get API key (free tier)
GEMINI_MODEL=gemini-3.8-flash  # pin it; the -latest alias 503'd under load
GEMINI_FALLBACK_MODEL=gemini-flash-lite-latest  # separate quota; used when the primary keeps failing
